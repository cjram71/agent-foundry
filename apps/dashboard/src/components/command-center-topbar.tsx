'use client';
import Link from'next/link';import{useRouter}from'next/navigation';import{FormEvent}from'react';import EmergencyStop from'./emergency-stop';
export default function CommandBar(){const router=useRouter();function search(event:FormEvent<HTMLFormElement>){event.preventDefault();const data=new FormData(event.currentTarget),query=String(data.get('q')||'').trim();if(query)router.push(`/search?q=${encodeURIComponent(query)}`)}return <div className="command-bar"><form className="global-search" onSubmit={search}><span>⌕</span><input name="q" aria-label="Search Gizmo" placeholder="Search missions, projects, tasks, agents…"/><kbd>⌘ K</kbd></form><div className="command-actions"><Link className="button primary" href="/missions?new=1">+ New Mission</Link><EmergencyStop/></div></div>}
