import { requireDashboardAdmin } from '@/lib/dashboard/auth'; import { loadServiceHealth } from '@/lib/dashboard/services'; import { Empty, OpsPage } from '@/components/ops-shell';
export const dynamic='force-dynamic';
export default async function Workflows(){await requireDashboardAdmin();const n8n=(await loadServiceHealth()).find(s=>s.id==='n8n');return <OpsPage eyebrow="INTELLIGENCE" title="Workflows" description="n8n remains the deterministic workflow system; the editor and credentials are not reproduced here."><div className="metric-grid"><article className="metric-card"><span>n8n</span><strong>{n8n?.status??'Unknown'}</strong></article><article className="metric-card"><span>Executions</span><strong>Unavailable</strong></article><article className="metric-card"><span>Failures</span><strong>Unavailable</strong></article></div><section className="panel"><Empty>n8n execution history is not integrated through an authorized adapter. No workflow run data is displayed.</Empty></section></OpsPage>}
