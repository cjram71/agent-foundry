import fs from'node:fs/promises';import path from'node:path';import prisma from'@/lib/prisma';import{deriveAgentState}from'./status';import type{AgentSummary}from'./types';
type RegistryAgent={id:string;name:string;status:string;models?:{primary?:string};budget?:{maximumDailyCostUsd?:number}};
export async function loadAgents(now=new Date()):Promise<AgentSummary[]>{const registry=JSON.parse(await fs.readFile(path.join(process.cwd(),'../../config/agents/registry.json'),'utf8'))as RegistryAgent[];const tasks=await prisma.task.findMany({where:{assignedAgent:{not:null},state:{notIn:['COMPLETED','REJECTED','CANCELLED','SECURITY_BLOCKED']}},orderBy:{updatedAt:'desc'},include:{missionLink:{include:{mission:{select:{id:true,goal:true}}}},attempts:{orderBy:{attemptNumber:'desc'},take:1},agentRuns:{orderBy:{createdAt:'desc'},take:1}}});return registry.map(agent=>{const task=tasks.find(row=>row.assignedAgent===agent.id||row.assignedAgent===agent.name)||null,run=task?.agentRuns[0]||null,attempt=task?.attempts[0]||null;return{id:agent.id,name:agent.name,registryStatus:agent.status,runtimeState:deriveAgentState({taskState:task?.state,runStatus:run?.status}),currentMission:task?.missionLink?.mission||null,currentTask:task?{id:task.id,title:task.title}:null,agentRunId:run?.id||null,elapsedSeconds:task?.startedAt?Math.max(0,Math.floor((now.getTime()-task.startedAt.getTime())/1000)):null,model:run?.model||agent.models?.primary||null,attempt:attempt?.attemptNumber||null,costUsd:task?Number(task.estimatedCost):null,tokens:run?.tokenUsage??null,lastActivity:run?.createdAt.toISOString()||task?.updatedAt.toISOString()||null}})}
