import tools from '../../../../../../config/tools/registry.json'; import { requireDashboardAdmin } from '@/lib/dashboard/auth'; import { OpsPage } from '@/components/ops-shell';
export const dynamic='force-dynamic';
export default async function Skills(){await requireDashboardAdmin();return <OpsPage eyebrow="INTELLIGENCE" title="Skills & Tools" description="The current deployment has an authoritative tool registry but no persisted skill certification or usage ledger."><section className="panel table-panel"><div className="table"><div className="table-row table-head"><span>Capability</span><span>Risk</span><span>Executor</span><span>Approval</span><span>Usage</span></div>{tools.map(t=><div className="table-row" key={t.id}><span><strong>{t.id}</strong><small>{t.description}</small></span><span>{t.risk}</span><span>{t.executor}</span><span>{t.approvalRequired?'Required':'Not required'}</span><span>Unknown</span></div>)}</div></section></OpsPage>}
