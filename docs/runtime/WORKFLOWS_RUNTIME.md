# Workflow Factory runtime

Workflow contracts are scoped, versioned, validated, approval-gated, audited, and idempotent. Deterministic filters and normalization remain separate from AI decision steps. Disabled, malformed, unapproved, and duplicate invocations fail closed.

