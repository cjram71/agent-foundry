# Skill Foundry runtime

The registry accepts only validated, certified, immutable skill versions. Certification requires sandbox success, security review, and every declared eval suite. Registry listing loads only metadata; full instructions load after selection and permission checks.

