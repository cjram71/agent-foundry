import Link from 'next/link';
import { notFound } from 'next/navigation';
import prisma from '@/lib/prisma';
import TaskActions from '@/components/task-actions';

export const dynamic='force-dynamic';
type Plan={summary?:string,selectedAgents?:Array<{catalogId?:string,name?:string,reason?:string,responsibilities?:string[]}>,catalogSource?:{repository?:string,commit?:string},steps?:Array<{order?:number,title?:string,description?:string,files?:string[],validation?:string}>,risks?:string[],acceptanceCriteria?:string[]}
export default async function TaskDetail({params}:{params:Promise<{id:string}>}){const {id}=await params;const task=await prisma.task.findUnique({where:{id},include:{project:true,agentRuns:{orderBy:{createdAt:'desc'}},approvals:{orderBy:{requestedAt:'desc'}}}});if(!task)notFound();const planner=task.agentRuns.find(r=>r.role==='planner'&&r.status==='success'&&r.outputSummary);let plan:Plan|null=null;try{plan=planner?.outputSummary?JSON.parse(planner.outputSummary):null}catch{}return <div className="page-stack"><header className="page-header"><div><Link className="back" href="/tasks">← Tasks</Link><h1>{task.title}</h1><p>{task.project.name} · {task.project.githubOwner}/{task.project.githubRepo}</p></div><span className={`badge large status-${task.status}`}>{task.status.replaceAll('_',' ')}</span></header><TaskActions taskId={task.id} status={task.status}/><section className="grid-two detail-grid"><div className="page-stack"><div className="panel"><div className="panel-head"><h2>Instruction</h2><span className={`risk risk-${task.riskLevel}`}>{task.riskLevel} risk</span></div><p className="instruction">{task.completeInstruction}</p></div>{plan&&<div className="panel plan"><div className="panel-head"><div><h2>Proposed implementation plan</h2><p>Generated by Gemini · requires human approval</p></div><span className="badge status-awaiting_plan_approval">Read only</span></div><p className="plan-summary">{plan.summary}</p>{plan.selectedAgents?.length?<div className="plan-box success"><strong>Selected agent team</strong><ul>{plan.selectedAgents.map((agent,i)=><li key={`${agent.catalogId}-${i}`}><a href={`https://github.com/cjram71/500-AI-Agents-Projects/tree/${plan.catalogSource?.commit||''}/agents/${agent.catalogId}`} target="_blank" rel="noreferrer"><strong>{agent.name||agent.catalogId}</strong></a><p>{agent.reason}</p>{agent.responsibilities?.length?<small>Responsibilities: {agent.responsibilities.join(' · ')}</small>:null}</li>)}</ul>{plan.catalogSource?.commit?<small>Catalog: cjram71/500-AI-Agents-Projects · {plan.catalogSource.commit.slice(0,12)}</small>:null}</div>:null}<ol>{plan.steps?.map((s,i)=><li key={`${s.order}-${i}`}><strong>{s.title}</strong><p>{s.description}</p>{s.files?.length?<small>Files: {s.files.join(', ')}</small>:null}<small>Validation: {s.validation}</small></li>)}</ol>{plan.risks?.length?<div className="plan-box"><strong>Risks</strong><ul>{plan.risks.map((r,i)=><li key={i}>{r}</li>)}</ul></div>:null}{plan.acceptanceCriteria?.length?<div className="plan-box success"><strong>Acceptance criteria</strong><ul>{plan.acceptanceCriteria.map((c,i)=><li key={i}>{c}</li>)}</ul></div>:null}</div>}{planner?.outputSummary&&<details className="panel"><summary>Raw manager result</summary><pre className="code-block">{planner.outputSummary}</pre></details>}<div className="panel"><div className="panel-head"><h2>Agent runs</h2><span>{task.agentRuns.length}</span></div>{task.agentRuns.map(r=><div className="timeline-row" key={r.id}><span className="timeline-dot"/><div><strong>{r.role} · {r.model}</strong><small>{r.status} · {r.tokenUsage.toLocaleString()} tokens · {new Date(r.createdAt).toLocaleString()}</small>{r.outputSummary&&r.role!=='planner'&&<p>{r.outputSummary}</p>}{r.errorInfo&&<p className="error-text">{r.errorInfo}</p>}</div></div>)}{!task.agentRuns.length&&<div className="empty">No agent has run this task yet.</div>}</div></div><aside className="page-stack"><div className="panel facts"><h2>Task details</h2><dl><dt>Status</dt><dd>{task.status.replaceAll('_',' ')}</dd><dt>Agent</dt><dd>{task.assignedAgent||'Not assigned'}</dd><dt>Branch</dt><dd>{task.branchName||'Not created'}</dd><dt>Tokens</dt><dd>{task.tokenUsage.toLocaleString()}</dd><dt>Estimated cost</dt><dd>${task.estimatedCost.toFixed(2)}</dd><dt>Created</dt><dd>{new Date(task.createdAt).toLocaleString()}</dd></dl>{task.pullRequestUrl&&<a className="button secondary" href={task.pullRequestUrl}>Open pull request</a>}{task.previewUrl&&<a className="button secondary" href={task.previewUrl}>Open preview</a>}</div><div className="panel"><div className="panel-head"><h2>Approvals</h2><span>{task.approvals.length}</span></div>{task.approvals.map(a=><div className="approval-mini" key={a.id}><strong>{a.approvalType}</strong><span className={`badge decision-${a.decision}`}>{a.decision}</span><small>{new Date(a.requestedAt).toLocaleString()}</small></div>)}{!task.approvals.length&&<div className="empty">No approval requests.</div>}</div></aside></section></div>}
